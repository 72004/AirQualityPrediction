name: Weekly AQI Model Training

on:
  schedule:
    # Runs every Sunday at 00:00 UTC (you can adjust as needed)
    - cron: "0 0 * * 0"
  workflow_dispatch:  # Allow manual run from GitHub Actions tab

jobs:
  train-model:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Python
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # 3️⃣ Install dependencies
      - name: Install dependencies
        working-directory: ${{ github.workspace }}
        run: |
          python -m pip install --upgrade pip
          pip uninstall -y sklearn scikit-learn || true
          pip install -r requirements.txt
          echo "✅ Installed packages:"
          pip list | grep scikit-learn || true

          
      # 4️⃣ Set environment variables
      - name: Set environment variables
        env:
          HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
        run: echo "✅ Environment variables loaded"

      # 5️⃣ Run the training script
      - name: Train AQI model
        run: |
          echo "🚀 Starting model training..."
          python train_aqi_model.py
          echo "✅ Training completed successfully."